// ============================================================
// ui/topbar.js — Topbar / navbar (currently static HTML;
//                this module is reserved for any dynamic
//                topbar behaviour)
// WebGIS Arkeologi Vertikal · Kotagede
// ============================================================

/**
 * Placeholder initialiser for topbar dynamic logic.
 * The topbar HTML is rendered statically in index.html.
 * This function is called from app.js during boot to allow
 * future topbar behaviour to be added here without touching
 * other modules.
 */
export function initTopbar() {
  // Static topbar — no dynamic initialisation required at this time.
  // All topnav click events are handled by eventHandler.js.
}
